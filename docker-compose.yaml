version: '3.8'
services:
  processor:
    build: ./processor
    devices:
      - /dev/video0:/dev/video0
    depends_on:
      - analyzer

  analyzer:
    build: ./analyzer
    ports:
      - "5000:5000"